#ifndef _RING_H
#define _RING_H
#include "Graph.h"
#include "Hash.h"
sHash *Graph_Ring(sGraph *g,const int size);
sHash *Graph_Ring2(sGraph *g,const int size);
sHash *Graph_Ring4(sGraph *g,const int size);
#endif
